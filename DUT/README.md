# DUT (Device Under Test) 🕵️

* DUT is the actual hardware chip needs to be tested.  
* For the HIL board, it appears as a black box (any Hardware/firmware).  
* The HIL simulator is only interested in the DUT response to the triggered action.
* you can flash vanilla baremetal examples using arduino for example just to verify the project working.
